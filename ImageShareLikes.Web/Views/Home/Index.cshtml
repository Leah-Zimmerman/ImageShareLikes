@model List<Image>
@using ImageShareLikesEF.Data;

<div class="row">
    <div class="col-md-6 offset-md-3">
        @*@foreach (var image in Model)
        {
            <div class="p-4 shadow bg-light mb-3" style="margin-bottom:20px; text-align:center">
                <a href="/home/viewimage?id=@image.Id">
                    <h3>@image.Title</h3>
                    <h5>@image.UploadDate</h5>
                    <img style="width:400px;" src="/uploads/@image.FileName">
                </a>
            </div>
        }*@
    </div>
</div>

@section scripts{
<script src="\js\index.js"></script>
}